#include <stdio.h>

/**
 * main - print all possible combination of single digit number
 * Return: Always 0
 */
int main(void)
{
	int la;

	for (la = 48; la < 58; la++)
	{
		putchar(la);
		if (n != 57)
		{
			putchar(',');
			putchar(' ');
		}
	}
	putchar('\n');
	return (0);
}
